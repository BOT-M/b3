#include "HandlerData.hpp"

namespace b3
{
    HandlerData::HandlerData(/* args */)
    {
    }

    HandlerData::~HandlerData()
    {
    }

    void HandlerData::analyzeData(const char* data, const std::size_t size)
    {
        
    }
}